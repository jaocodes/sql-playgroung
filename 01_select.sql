SELECT 'Olá Mundo!';

SELECT 1 + 1 + 10;

SELECT * FROM clientes;

SELECT * FROM clientes LIMIT 3;

SELECT idCliente, QtdePontos FROM clientes;

SELECT idCliente, DtCriacao, QtdePontos FROM clientes LIMIT 5;
